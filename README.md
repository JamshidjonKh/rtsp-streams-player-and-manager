# Node.js + Express + React RTSP Streams manager and player


[DEMO ON HEROKU]()

# Usage

Install all dependencies

```sh
npm i
```

Run the server(port: 5000) and client(port: 3000)

```sh
npm run dev
```
